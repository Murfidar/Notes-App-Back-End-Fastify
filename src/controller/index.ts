import {notes, note} from '../data/notes' 

export const getAllNotes = async (request, reply) => {
  
}

export const getNoteById = async (request, reply) => {
  
}

export const updateNoteById = async (request, reply) => {
  
}

export const deleteNoteById = async (request, reply) => {
  
}